# Task 21 暂停 checkpoint（2026-09-14）

## 当前边界与剩余量

用户最新要求暂停、核对剩余任务，并保存 checkpoint。本轮不再进入 Task 22。Task 1–20 已完成并经过测试、规格审查和质量审查；已验证功能基线为 `4ef174656f5d8b7adb89e30dc12e7a4de07d4c14`，已推送 main。历史验证见 [Task 16–20 记录](2026-09-14-remaining-tasks.md)。

剩余 **2 个任务**，不能把 20/22 的任务数量比例等同于剩余工时：

- Task 21：进行中。隔离 E2E harness 的 health 已通过，行为 RED 已建立；Fake scenarios 尚未实现，完整 E2E 未通过，尚未进行两阶段审查。
- Task 22：未开始实施。中文交付文档、隔离全量集成测试 target、完整验证、fresh clone 和第二独立 Docker 环境验收仍待完成。

实现代理因账户额度上限中断，不能宣称当前 Task 21 已完成。本次只保存可恢复的工作现场。未通过的实现和红测保留在开发分支，不合入 main；main 只更新 checkpoint 文档，保持已验证功能基线。

## Git 与恢复位置

- 主目录：`/Users/mei/Desktop/Project/harness-forge`，`main`。
- 开发工作树：`/Users/mei/Desktop/Project/harness-forge/.worktrees/v0-implementation`，`feat/v0-implementation`。
- Task 21 WIP 提交：`b7f5e4be1ffbdd0e454051999ac9f7315edc9702`，`wip: checkpoint task 21 harness and red scenario tests`。
- 本 checkpoint 提交后，将 main 的文档合入开发分支，再推送两个分支；不要将红测开发分支反向合入 main。

下次用户说“继续”时，先阅读本文件，再进入已有工作树，不重新创建仓库或重复 Task 1–20。优先完成 Task 21，再按当时用户授权决定是否进入 Task 22。

```bash
cd /Users/mei/Desktop/Project/harness-forge/.worktrees/v0-implementation
git status --short
git log -5 --oneline
GOTOOLCHAIN=local env -u GOROOT go -C services/control-plane test ./internal/sandbox -run FakeScenario -count=1 -v
```

最后一条在本 checkpoint **预期失败**，不是新的环境损坏；它是待实现功能的 RED。

## Task 21 已保存内容

1. Compose 的全部 host ports 支持变量，默认开发端口不变。
2. `make test-e2e` 使用独立 `harness-forge-e2e` project；Web 15173、Go 18080、Artifact 18081、Runtime 18090、Postgres 15432、MinIO 19000/19001。
3. 空 env-file、显式 Fake/空 Claude 凭证，固定内部连接和路径；绝对 Compose 路径与退出 trap；拒绝不明既有同名资源。完整套件失败后也已清理该 project 的容器、网络和 volumes，父级独立确认三类资源均为空，无残留 Playwright/Make 进程。
4. `tests/e2e` 锁定 Playwright 1.54.1、TypeScript 5.8.3；Chromium 已下载。health 基线代理报告 1/1 通过。
5. 真实浏览器→Go 行为测试：上传/两个会话/两个版本、延迟重载、阻塞与取消、失败保留旧制品。没有 route.fulfill 或 API mock；重放验证只读实际 Pinia RunStore，不修改业务状态。
6. Go FakeScenario 红测、HTTP Runtime 保留原始 fixture 前缀测试。`fake.go` 和所有场景 fixture 尚未修改，仍只有原 geo-report。

父级重新执行 FakeScenario 红测，确认失败原因：success-v2 仍输出 v1、agent-failure 仍 completed、invalid-manifest 仍有效、未知 selector 未拒绝、1000ms 延迟缺失、blocking 仍发送 completed。完整 E2E 的 `.last-run.json` 记录 4 个行为失败，保留 trace/截图于工作树 `tests/e2e/test-results`（被 Git 忽略；恢复不依赖这些本地输出）。`git diff --check` 通过。

## Task 21 下一步

按[实施计划 Task 21](../plans/2026-07-19-harness-forge-v0.md)继续，不跳过已建立的 RED，也不把连接失败当作行为红测：

1. 实现仅 Fake 识别的白名单 `[fixture:<scenario>]`；普通 prompt 仍默认 geo-report，HTTP Docker Runtime 不解析前缀。
2. 添加 success-v2、agent-failure、invalid-manifest、delayed-success、blocking；schema 为 `{version:1, base_fixture:string, event_delay_ms:uint, block_before_type:string|null, release:"none"|"context_cancel"}`。
3. delayed-success 基于 geo-report，每 event 延迟 1000ms；blocking 延迟 50ms，阻塞在 agent.completed 前直到 context cancel，取消后不得发送 terminal。
4. 成功 HTML 引用同 prefix 的本地 ECharts stub；真实 ECharts 已在 Task 16 验证，不以 stub 替代真实库验证。
5. 补齐**精确 delay hook** 回归，目前红测只有实际时钟等待；加强 E2E 证明 reload 真正在 tool.started 与下一 event 的 1000ms 间隔内，不只验证最终 API 历史。
6. 执行 Go targeted → 完整 `make test` → `make test-e2e` 全套连续两次；核对资源清理、规格审查、质量审查、父级独立验收。必要时对用例本身做最小修正，不削弱断言。
7. 更新计划/本 checkpoint，全部通过后才合并功能到 main。

## Task 22 已定位的验证注意事项

- `TEST_MINIO_ENDPOINT` 必须完整 URL，如 `http://localhost:29000`；裸 `localhost:29000` 在 cleanup helper 中解析为 Host 空，父级已复现并纠正。
- 全量 integration 使用 `go test -p 1` 按包执行：不同 schema 仍共用数据库级 `harness-forge:artifact-maintenance` advisory lock，跨包并行会干扰 lock availability 断言和断连注入。保留测试内部并发及 race 检查，不更改生产锁。
- 真实共享目录权限测试必须设置 `HF_PERMISSIONS_INTEGRATION=1 HF_COMPOSE_PROJECT=<隔离 project>`，只有 `COMPOSE_PROJECT_NAME` 会 skip。
- 在 main `4ef1746` 上，父级显式隔离连接、`-p 1 -tags=integration -race ./internal/... -count=1` 全包通过；两个 publication lock 用例另以 `-count=20` 通过。原并行运行曾出现锁可用性误报，未隐瞒为全绿。
- Task 22 的 clean verify、integration、E2E 全部使用独立 project/端口、空凭证和完整内部连接覆盖；trap 不能因 `cd` 丢失 Compose 路径。
- 第二独立 Docker machine/干净 CI runner 仍未提供。此前已询问允许添加/运行 GitHub Actions，尚无答复；未添加或触发 CI。没有第二环境不能勾选 Task 22 完成。

## 环境与安全边界

macOS arm64；Go 1.25.4（宿主 GOROOT 需 unset）、Node 25.6.1、pnpm 9.15.4、uv 0.6.6；Docker 29.4.0 linux/arm64、Compose 5.1.2。Python 测试 253 项；已验证 Task 20 前端 108 项、Node smoke 脚本测试 17 项。已有 Starlette/httpx deprecation 和 Node localStorage warning，未因本任务新增依赖修复。

父级临时 `hf-full-20260912` 栈用于 Task 20 与集成验证；测试 Project 已 purge，无 orphan。本次暂停时停止该栈，保留 volumes/images，不触碰默认开发数据。E2E 栈已完整清理。

未读取宿主 Claude 凭证/配置，未执行真实 Claude query/CLI/API；真实 smoke 仍人工 opt-in。固定 Python Agent SDK 0.2.120 与 bundled CLI 2.1.211 不升级，不增加 E2B adapter 或跨 Provider Session 迁移。
