# Harness Forge 实施检查点（2026-08-01）

## 当前结论

- Task 1–4 已完成规格审查、质量审查和控制器复验。
- Task 5 的主体实现已提交，但规格审查发现 3 个 Important；修复已保存为 WIP，尚未完成真实 PostgreSQL 集成复验和双审查。
- Task 6 尚未开始。
- 下一次对话直接说“继续”，应从本文件的“恢复步骤”开始，不要重新实现 Task 1–4。

## 关键提交

- `35841db`：修复 Web IPv4 healthcheck；Task 3 真实 Compose 验证通过。
- `f1444f3`、`f9f7449`、`1263e4a`：Task 4 migration、连接池生命周期和 `profile_version=text` 修复。
- `dccab8e`：Task 5 Project、Input File、Profile Resolver、MinIO/Memory object store 和 HTTP 主体实现。
- `b105672`：Task 5 规格审查问题的未完成 WIP 修复检查点。

## Task 5 已确认的主体能力

- immutable Profile snapshot、prompt/template/config digest。
- Project Create/Read/Rename/List/Delete。
- Input File multipart 上传、流式 SHA-256/size 计算、最终对象 key 和失败补偿。
- concrete pgx Project store、MinIO/Memory object store。
- Project/Input HTTP routes、组合根和仓库根 Docker build context。
- 在 `dccab8e` 上曾通过 unit、HTTP、PostgreSQL race、Compose 和真实 HTTP/MinIO/DB 链路验证。

## 规格审查发现的 3 个 Important

1. `max_budget_usd` 应允许缺省或 `0`，只拒绝负值。
2. multipart 只允许一个名为 `file` 的文件 part；未知字段、重复文件和缺失文件必须返回统一 `400 Error`，不能静默忽略。
3. upload-first 删除竞态必须真正并发，并用 barrier 证明 Delete 被 Upload 持有的 Project row lock 阻塞；纯顺序测试是假绿。

`b105672` 已包含针对这三项的代码和测试修改，但代理被用户要求立即 checkpoint 时中断。中断后仅重新验证：

```text
go -C services/control-plane test ./... -count=1   PASS
git diff --check                                   PASS
```

不要把 Task 5 标记为完成，直到下面步骤全部通过。

## 恢复步骤

工作目录：

```bash
cd /Users/mei/Desktop/Project/harness-forge/.worktrees/v0-implementation
git status --short --branch
```

1. 检查 `b105672` 的 7 个文件差异，确认 multipart reader 在对象上传失败时不会误报成功，race test 的 test-only hook 不泄漏为公共 API。
2. 启动依赖并运行非缓存集成测试：

```bash
docker compose -f docker-compose.yaml up -d --wait postgres minio
TEST_DATABASE_URL='postgres://harness_forge:local-dev-only@localhost:5432/harness_forge?sslmode=disable' \
  env -u GOROOT go -C services/control-plane test -tags=integration -race ./internal/projects -count=1 -v
```

3. 运行目标测试与静态检查：

```bash
env -u GOROOT go -C services/control-plane test ./internal/profiles ./internal/projects ./internal/httpapi -run 'Profile|Project|InputFile' -count=1 -v
env -u GOROOT go -C services/control-plane test ./... -count=1
env -u GOROOT go -C services/control-plane vet ./...
git diff --check
docker compose -f docker-compose.yaml down --remove-orphans
```

4. 重新做 Task 5 规格审查，重点复核上述 3 项；有问题继续修复并复审。
5. 规格通过后再做独立质量审查；修复所有 Critical/Important 并复审。
6. 控制器做真实 Compose/HTTP/PostgreSQL/MinIO 最终验证，Task 5 才可标记完成。
7. 然后开始计划中的 Task 6：Run 状态机、持久队列与 Event store。

## 环境提示

- OrbStack 已恢复，Docker Server 可用。
- Task 4 的 `00001_initial.sql` 已修为 `projects.profile_version text`。旧的默认本地 PostgreSQL volume 可能保留评审期间的旧 schema；公共 schema E2E 应使用独立 Compose project/新卷，避免假阳性。
- 不要修改已提交的 `00001_initial.sql`；Task 6 按计划新增 `00002_run_event_sequences.sql`。
- 不要在 Task 5 恢复期间提前实现 Task 6。
