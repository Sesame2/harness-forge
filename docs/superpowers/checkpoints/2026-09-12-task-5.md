# Harness Forge 实施检查点（2026-09-12）

## 恢复结论

- Task 1–5 已完成；Task 5 规格审查、质量审查及真实服务验证已通过。
- Task 6 尚未开始。下次说“继续”时，从 Run 状态机、持久队列与 Event store 开始，不要重新实现 Task 1–5。
- 此次仅完成当前 Task 并保存 checkpoint；完整地理分析、会话 UI、Claude 执行和 E2B adapter 尚未完成，依照后续计划实施。
- 主仓库：`/Users/mei/Desktop/Project/harness-forge`；开发工作树：`.worktrees/v0-implementation`，分支 `feat/v0-implementation`。保留工作树以便恢复。
- 恢复时先检查 `git status`、`git log` 和分支关系；本检查点提交用于同步本地及远程 `main`，以实际 Git 状态为准。

## 本次完成内容

延续 `69304dd` 检查点，复核并验证 `b105672` 中三项修复：

1. `max_budget_usd` 允许缺省或 0，仅负数非法。
2. multipart 只允许一个名为 `file` 的文件 part；未知字段、重复或缺失文件返回统一 400，失败时删除准确的对象 key，不保存 metadata。
3. upload-first 删除竞态使用真实 PostgreSQL row lock 与并发等待屏障；测试 hook 保持私有。

质量复核另发现请求取消会导致对象补偿删除也被取消。本次提交 `d94f5d2` 修复：

- Put 失败、文件超限、metadata 保存失败三条路径共用私有补偿函数。
- 清理使用 `context.WithoutCancel` 加 5 秒独立超时，避免继承客户端断连，同时限制清理时长。
- 保留原始对象 key；使用 `errors.Join` 保留业务错误与清理错误。
- 新增 8 个确定性测试子用例，先观察失败再修复；独立质量复审通过。

补偿仍是有界的同步尽力清理：若对象存储故障或超时，返回组合错误，不承诺永久消除所有孤儿对象，也未增加后台重试队列。

## 最新验证证据

在 `d94f5d2` 源码上验证：

```bash
GOTOOLCHAIN=local env -u GOROOT go -C services/control-plane test -race ./... -count=1
GOTOOLCHAIN=local env -u GOROOT go -C services/control-plane vet ./...
TEST_DATABASE_URL='postgres://harness_forge:local-dev-only@localhost:5432/harness_forge?sslmode=disable' \
  GOTOOLCHAIN=local env -u GOROOT go -C services/control-plane test \
  -tags=integration -race ./internal/postgres ./internal/projects -count=1 -timeout=90s
git diff --check
```

结果均通过。集成测试包含 migration、CRUD/删除保护、upload-first 与 delete-first 两种竞态顺序。

真实 HTTP/PostgreSQL/MinIO 联调通过：

- multipart 未知字段（文件前/后）、重复文件、缺失文件均返回 400；无 metadata、无残留对象。
- 合法 CSV 上传返回 201；对象实际字节、最终 key、size、SHA-256 与 metadata 一致。
- 删除返回 204、重复删除幂等；删除后上传返回 404。逻辑删除保留既有输入对象符合当前设计。
- 额外连接锁住 Project row，HTTP 上传进入对象存储后主动断开客户端；确认最终无对象、无 metadata。
- control-plane、web、agent-runtime、postgres、minio 健康；minio-init 正常退出 0。
- 未调用真实 Claude API，未使用真实 Claude 凭证。

### Docker 验证边界

标准 `docker compose up --build` 本次受 Docker Hub token/基础镜像元数据请求网络超时阻断，不能声称已完成标准 Dockerfile 全量重建。

实际联调采用当前源码交叉编译的 Linux ARM64 控制平面二进制，覆盖到本机已有控制平面镜像，并复制当前 contracts/profiles；Web 镜像本次构建成功，Runtime 使用已有镜像（本次未改 Runtime）。随后以隔离 Compose project `hf-resume-20260912` 和新数据卷运行完整服务。未修改仓库 Dockerfile、网络配置或无关容器。

这证明当前控制平面源码的真实服务行为，不替代网络恢复后对原始 Dockerfile 的标准重建检查。临时验证脚本、override 和二进制位于本机工作目录 `~/Documents/Codex/2026-07-19/new-chat/work/task5-resume/`，不属于项目依赖，恢复无需依赖这些文件。

## 下一步：Task 6

阅读[实施计划](../plans/2026-07-19-harness-forge-v0.md)的 Task 6，并对照[中文规格](../specs/2026-07-19-harness-forge-design.zh-CN.md)中的状态与 finalization 矩阵。

1. 先写纯状态转换/终态矩阵失败测试，再实现 `internal/runs` model/state；明确 Acquire 不确定、Lease/Runtime disposition、Release 与 `finalized_at` 的关系。
2. 写真实 PostgreSQL queue/Event 失败测试，包含双连接并发 claim 屏障及幂等性。
3. 新建 `migrations/00002_run_event_sequences.sql`，不要修改已执行的 `00001_initial.sql`。
4. 实现 durable sequence、runtime sequence 幂等去重、事务内建 Run、Sandbox provider/ref 记录及全局单 Run claim。
5. 按计划执行测试、规格审查、质量审查与真实数据库复验，再提交并更新 checkpoint。

继续沿用小步实现；V0 只实现计划要求的 Docker/Fake Provider，保留 E2B adapter 边界，不提前实现 E2B。

## 环境提醒

- OrbStack/Docker 已可用；不要停止无关 Kubernetes 容器或改动全局 Docker 配置。
- 本次隔离验证的 `hf-resume-20260912` 容器、网络及新建卷已清理；用户原有容器和卷未动。
- 旧默认 Compose PostgreSQL 卷可能仍有评审期间的旧 schema。新环境验证使用独立 Compose project 和新卷；禁止为测试清空用户原有卷。
- 测试命令使用 `env -u GOROOT` 避免本机 Go 环境变量干扰。
- 实施计划中历史 checkbox 尚未逐项回填；任务级恢复状态以本 checkpoint 和 Git 提交为准。
